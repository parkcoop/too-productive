import {Layout, Text} from '@ui-kitten/components';
import React from 'react';

const Login = () => {
  return (
    <Layout style={{flex: 1}}>
      <Text>LOGIN</Text>
    </Layout>
  );
};

export default Login;
